int main(void)
{
	return 0;
	
}


/*
	注意
	1.老版本的 keil 和 Windows 的中文编码是不一样的, 
	·老版 keil 默认用的编码是 ANSI 编码, 中文占 2 个字节
	·keil 的编辑器按字节处理，识别一个中文为 2 个字节，所以删除一次只能删半个字
	·所以在 keil 中删除的时候, 删除两次才算是删除掉一个汉字

	2.在 keil 中凡是自己创建的文件, 在代码最后一定要加几行空行(回车)
	·C语言标准规定：源文件应该以换行符结束
	·老版本编译器在处理最后一行没有换行符的文件时，可能产生警告
	·某些编译器在预处理器处理时可能出现问题
	·避免 "no newline at end of file" 编译警告

	3.在封装函数的时候, 如果函数没有参数, 必须写 void

	4.在 keil 中变量的定义, 一定要放在函数的开头位置
	·老版本 Keil 使用的是 C89 标准，要求所有局部变量必须在函数开头定义
	·C99 标准允许混合放置（在使用前定义即可），但老 Keil 编译器不支持
*/

